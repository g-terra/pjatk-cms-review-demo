'use strict';

/**
 * user-available-file service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::user-available-file.user-available-file');
